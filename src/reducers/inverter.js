import {ADD} from '../actions/solarPanel/types'
const initState = {
    items: [],
}
const geographic = (state = initState, {type, payload})=> {
    switch (type) {
        default:
            return state
    }
}
export default geographic
